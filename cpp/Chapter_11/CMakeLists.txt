if(${WITH_HPX})
	if(${WITH_HPX_COROUTINE})
		add_hpx_executable(Listing_11_2 SOURCES Listing_11_2.cpp)
	endif()
endif()
if(${WITH_HPX})
	if(${WITH_HPX_COROUTINE})
		add_hpx_executable(Listing_11_3 SOURCES Listing_11_3.cpp)
	endif()
endif()
if(${WITH_HPX})
	if(${WITH_HPX_COROUTINE})
		add_hpx_executable(Listing_11_1 SOURCES Listing_11_1.cpp)
	endif()
endif()
